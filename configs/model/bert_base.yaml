# BERT-base Model Configuration
name: "google-bert/bert-base-uncased"
vocab_size: 30522
hidden_size: 768
task: "sequence_classification"

# IASP Configuration
iasp:
  target_layer_name: "bert.encoder.layer.0.attention.self.query"
  cluster_size_range: [8, 64] 
