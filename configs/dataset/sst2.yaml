# SST-2 Dataset Configuration  
name: "sst2"
path: "glue"
subset: "sst2"
metric_type: "accuracy"
text_column: "sentence"
sample_size: 512
batch_size: 32
learning_rate: 2e-5
eval_split: "validation"
# Note: seq_len is handled by tokenizer's max_length for classification tasks
