# CPU-only testing dependencies (reduced disk usage)
# Install PyTorch CPU version first to avoid CUDA dependencies
--index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<2.5.0

# Core testing dependencies
pytest>=8.0.2
pytest-cov

# Minimal core dependencies for testing with Mamba support
numpy>=1.21.0
scikit-learn>=1.3.0
transformers>=4.39.0  # Mamba support since 4.39.0
datasets>=2.14.0
pyyaml>=6.0
hydra-core>=1.3.0
omegaconf>=2.3.0

# Mamba dependencies for testing (CPU fallback)
# Note: These will use slower implementations without CUDA
causal-conv1d>=1.2.0
mamba-ssm>=1.2.0
