# CPU-only testing dependencies (reduced disk usage)
# Note: Install PyTorch CPU version separately with:
# pip install torch>=2.0.0,<=2.3.1 --index-url https://download.pytorch.org/whl/cpu

# Core testing dependencies
pytest>=8.0.2
pytest-cov

# Minimal core dependencies for testing (stable versions)
numpy>=1.21.0
scikit-learn>=1.3.0
transformers>=4.42.4  # Unified version
datasets>=2.14.0
pyyaml>=6.0
hydra-core>=1.3.0
omegaconf>=2.3.0

# Note: causal-conv1d and mamba-ssm require CUDA/nvcc to build
# They are not included in test dependencies to allow CPU-only testing
# For full functionality, install them separately in a CUDA environment
