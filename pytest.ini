[pytest]
addopts = -q --basetemp=./.pytest_tmp
markers =
  cuda: requires CUDA GPU
  perf: deterministic performance smoke tests for the mock pipeline
  stability: failure-path regression tests covering retries and rollbacks
