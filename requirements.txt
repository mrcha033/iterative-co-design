# Core dependencies (install order matters!)
numpy>=1.21.0
torch>=2.0.0,<=2.3.1  # Compatible with CUDA 12.1

# Basic ML dependencies  
scikit-learn>=1.3.0
pandas>=1.5.0
tqdm>=4.64.0
pyyaml>=6.0

# Configuration management
hydra-core>=1.3.0
omegaconf>=2.3.0

# ML tracking and visualization
wandb>=0.15.0
matplotlib>=3.5.0
seaborn>=0.11.0

# Transformers (use stable version first)
datasets>=2.14.0
transformers>=4.42.4  # Use stable version compatible with current PyTorch

# Testing
pytest>=8.0.2
mkdocs>=1.5.3
mkdocs-material>=9.5.13
mkdocstrings[python]>=0.24.1
ruff>=0.12.0

# Optional Mamba dependencies (install separately if needed)
# causal-conv1d>=1.2.0
# mamba-ssm>=1.2.0
